node_modules/
.env
*.log
.DS_Store
npm-debug.log*

# Runtime data — db files excluded, seed JSON files are committed
data/*.db
data/*.db-shm
data/*.db-wal

# data/seed-experiences.js is intentionally committed (needed at deploy time)

.vercel
idx_test.js

# APK builds tracked in git for Vercel static serving
# public/downloads/*.apk
